# Automatic Guitar Chord Transcription

This project compares a classical signal-processing pipeline with a deep-learning model
for automatic chord transcription from audio.

## Installation

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Running the classical baseline

python scripts/run_classical.py
